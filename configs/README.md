# Variables

For this project, there is only one mandatory variable. Make sure to set storage_dir in [the config file](private_vars.yaml) to a directory where you want to store the data and models. This directory will be used for all data storage, including training data, model checkpoints, and logs.
